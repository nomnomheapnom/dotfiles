$mainMod = SUPER

# ── Applications ──────────────────────────────────────────────────────────────
bindd = $mainMod, RETURN, Terminal, exec, ghostty
bindd = $mainMod ALT, RETURN, Terminal (cwd), exec, ghostty --working-directory="$HOME"
bindd = $mainMod, N, Neovim, exec, ghostty -- nvim
# Changed from T to ALT T to avoid conflict with floating toggle
bindd = $mainMod ALT, T, System monitor, exec, ghostty -- btop
bindd = $mainMod, D, Lazy Docker, exec, ghostty -- lazydocker

$browser = brave --force-dark-mode --enable-features=WebUIDarkMode

bindd = $mainMod SHIFT, RETURN, Browser, exec, $browser
bindd = $mainMod SHIFT, B, Browser, exec, $browser
bindd = $mainMod SHIFT ALT, B, Browser (private), exec, $browser --private-window

bindd = $mainMod SHIFT, F, File manager, exec, dolphin

bindd = $mainMod SHIFT, N, Editor, exec, ghostty -- nvim
bindd = $mainMod SHIFT, T, Floating terminal, exec, ghostty --class popup.term

# ── Web apps ──────────────────────────────────────────────────────────────────
bindd = $mainMod SHIFT, A, ChatGPT, exec, xdg-open "https://chatgpt.com"
bindd = $mainMod SHIFT, Y, YouTube, exec, xdg-open "https://youtube.com"
bindd = $mainMod SHIFT, X, X / Twitter, exec, xdg-open "https://x.com"
bindd = $mainMod SHIFT, E, Email, exec, xdg-open "https://mail.google.com"
bindd = $mainMod SHIFT, C, Calendar, exec, xdg-open "https://calendar.google.com"

# ── Wofi / App Launcher ────────────────────────────────────────────────────────
bindd = $mainMod, SPACE, Launcher, exec, wofi --show drun --allow-images

# ── Session ───────────────────────────────────────────────────────────────────
bindd = $mainMod, ESCAPE, Power menu, exec, ~/.config/wofi/powermenu.sh
bindd = $mainMod SHIFT, L, Lock screen, exec, hyprlock

bindd = $mainMod SHIFT, Q, Logout, exec, loginctl lock-session
bindd = $mainMod SHIFT, R, Reload config, exec, hyprctl reload
bindd = $mainMod, Q, Kill window, killactive
bindd = $mainMod, W, Kill window, killactive
bindd = $mainMod CTRL ALT, Q, Force kill window, exec, hyprctl kill
bindd = CTRL ALT, Delete, Close all windows, exec, ~/.local/bin/close-all-windows.sh

bindd = , PRINT, Screenshot (region), exec, grim -g "$(slurp)" - | tee ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy && notify-send "Screenshot Captured" "Region copied to clipboard and saved"
bindd = SHIFT, PRINT, Screenshot (fullscreen), exec, grim - | tee ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png | wl-copy && notify-send "Screenshot Captured" "Fullscreen copied to clipboard and saved"
bindd = $mainMod, PRINT, Screenshot (annotate), exec, grim -g "$(slurp)" - | satty --filename - --output-filename ~/Pictures/Screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png

bindd = $mainMod, INSERT, Color picker, exec, hyprpicker -a

# ── Audio & Media ──────────────────────────────────────────────────────────────
bindd = , XF86AudioRaiseVolume, Volume up, exec, swayosd-client --output-volume raise
bindd = , XF86AudioLowerVolume, Volume down, exec, swayosd-client --output-volume lower
bindd = , XF86AudioMute, Mute, exec, swayosd-client --output-volume mute-toggle
bindd = , XF86AudioMicMute, Mute mic, exec, swayosd-client --input-volume mute-toggle

# Fn + F1-F4
bindd = , code:67, Mute (F1), exec, swayosd-client --output-volume mute-toggle
bindd = , code:68, Volume Down (F2), exec, swayosd-client --output-volume lower
bindd = , code:69, Volume Up (F3), exec, swayosd-client --output-volume raise
bindd = , code:70, Unmute (F4), exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ 0 && swayosd-client --output-volume 0

bindd = , XF86AudioPlay, Play/pause, exec, playerctl play-pause
bindd = , XF86AudioPrev, Previous track, exec, playerctl previous
bindd = , XF86AudioNext, Next track, exec, playerctl next

bindd = , XF86MonBrightnessUp, Brightness up, exec, swayosd-client --brightness raise
bindd = , XF86MonBrightnessDown, Brightness down, exec, swayosd-client --brightness lower

# ── Notifications ──────────────────────────────────────────────────────────────
bindd = $mainMod SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = $mainMod ALT, COMMA, Invoke last notification, exec, makoctl invoke
bindd = $mainMod SHIFT ALT, COMMA, Restore last notification, exec, makoctl restore

# ── Window Focus & Movement ────────────────────────────────────────────────────
bindd = $mainMod, left, Focus left, movefocus, l
bindd = $mainMod, right, Focus right, movefocus, r
bindd = $mainMod, up, Focus up, movefocus, u
bindd = $mainMod, down, Focus down, movefocus, d

bindd = $mainMod SHIFT, left, Move window left, movewindow, l
bindd = $mainMod SHIFT, right, Move window right, movewindow, r
bindd = $mainMod SHIFT, up, Move window up, movewindow, u
bindd = $mainMod SHIFT, down, Move window down, movewindow, d

# ── Window States & Layout ─────────────────────────────────────────────────────
bindd = $mainMod, F, Fullscreen, fullscreen, 0
bindd = $mainMod, P, Toggle pseudotile, pseudo
bindd = $mainMod, S, Toggle split direction, layoutmsg, togglesplit

# ── Workspaces ────────────────────────────────────────────────────────────────
bindd = $mainMod, 1, Go to workspace 1, workspace, 1
bindd = $mainMod, 2, Go to workspace 2, workspace, 2
bindd = $mainMod, 3, Go to workspace 3, workspace, 3
bindd = $mainMod, 4, Go to workspace 4, workspace, 4
bindd = $mainMod, 5, Go to workspace 5, workspace, 5
bindd = $mainMod, 6, Go to workspace 6, workspace, 6
bindd = $mainMod, 7, Go to workspace 7, workspace, 7
bindd = $mainMod, 8, Go to workspace 8, workspace, 8
bindd = $mainMod, 9, Go to workspace 9, workspace, 9
bindd = $mainMod, 0, Go to workspace 10, workspace, 10

bindd = $mainMod SHIFT, 1, Move to workspace 1, movetoworkspace, 1
bindd = $mainMod SHIFT, 2, Move to workspace 2, movetoworkspace, 2
bindd = $mainMod SHIFT, 3, Move to workspace 3, movetoworkspace, 3
bindd = $mainMod SHIFT, 4, Move to workspace 4, movetoworkspace, 4
bindd = $mainMod SHIFT, 5, Move to workspace 5, movetoworkspace, 5
bindd = $mainMod SHIFT, 6, Move to workspace 6, movetoworkspace, 6
bindd = $mainMod SHIFT, 7, Move to workspace 7, movetoworkspace, 7
bindd = $mainMod SHIFT, 8, Move to workspace 8, movetoworkspace, 8
bindd = $mainMod SHIFT, 9, Move to workspace 9, movetoworkspace, 9
bindd = $mainMod SHIFT, 0, Move to workspace 10, movetoworkspace, 10

bindd = $mainMod, TAB, Next workspace, workspace, e+1
bindd = $mainMod SHIFT, TAB, Prev workspace, workspace, e-1

# Changed from MINUS to M to avoid conflict with resize bindings
bindd = $mainMod, M, Toggle scratchpad, togglespecialworkspace, magic
bindd = $mainMod SHIFT, M, Move to scratchpad, movetoworkspace, special:magic

# ── Mouse & Zoom ──────────────────────────────────────────────────────────────
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bindd = $mainMod CTRL, Z, Zoom in, exec, hyprctl keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float + 1')
bindd = $mainMod CTRL ALT, Z, Reset zoom, exec, hyprctl keyword cursor:zoom_factor 1

# ----------------------------------------------------- 
# Fullscreen & Maximizing
# ----------------------------------------------------- 
# 0 is true fullscreen, 1 is maximize (keeps gaps/bar)
bind = , F11, fullscreen, 0
bind = SHIFT, F11, fullscreen, 0 
bind = ALT, F11, fullscreen, 1

# ----------------------------------------------------- 
# Directional Resizing
# ----------------------------------------------------- 
# 'binde' allows the key to repeat when held down
# Syntax: resizeactive, X-pixels Y-pixels
binde = $mainMod, minus, resizeactive, -40 0
binde = $mainMod, equal, resizeactive, 40 0
binde = $mainMod SHIFT, minus, resizeactive, 0 40
binde = $mainMod SHIFT, equal, resizeactive, 0 -40

# ----------------------------------------------------- 
# Floating & Popping Out
# ----------------------------------------------------- 
bind = $mainMod, T, togglefloating

# In Hyprland, 'pin' makes a floating window sticky across all workspaces
bind = $mainMod, O, togglefloating
bind = $mainMod, O, pin

# Changed to ALT + S to avoid conflict with Music (Super + M) and Split (Super + S)
bindd = $mainMod ALT, S, Toggle scratchpad, togglespecialworkspace, magic
bindd = $mainMod ALT SHIFT, S, Move to scratchpad, movetoworkspace, special:magic
